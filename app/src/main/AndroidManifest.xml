<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cuny.ccny.csc221.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mtrack_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mtrack_icon_round"
        android:supportsRtl="false"
        android:theme="@android:style/Theme.Holo">
        <activity android:name="com.billassociates.mtrack.MetrocardEditActivity"></activity>
        <activity android:name="com.billassociates.mtrack.MainActivityOld">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>